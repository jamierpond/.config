#!/bin/zsh
# Start an interactive shell in a selected Docker image

chosen_instance=$(docker-image-select-fzf) || exit 1
cmd="docker run -it --entrypoint /bin/bash $chosen_instance"
print -s "$cmd" 2>/dev/null
eval "$cmd"
