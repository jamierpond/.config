#!/bin/bash
# Play a bell sound (works on macOS or remotely from Linux)

bell_file="$HOME/.config/bell.mp3"

if [[ "$(uname)" == "Darwin" ]]; then
  afplay "$bell_file" &
else
  mac -c "afplay {}" "$bell_file" &
fi
