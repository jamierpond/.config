#!/bin/bash
set -e
command="$1"
ssh_host="jamiepond@me.pond.audio"
ssh -o "StrictHostKeyChecking=accept-new" \
    -o "ProxyCommand=cloudflared access ssh --hostname %h" \
    $ssh_host -x "$command"
