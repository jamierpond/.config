#!/bin/zsh
# Select a GCloud project using fzf

projects=$(gcloud projects list)
project=$(echo "$projects" | fzf --height 40% --reverse --prompt "Select project: " --header-lines 1)

if [ -z "$project" ]; then
  exit 1
fi

echo "$project" | awk '{print $1}'
